const test = document.getElementById('traffic-div')

test.textContent = "test"
// Grab all the information from redis

// For each append to the screen

const trafficContainer = () => {
  const parent = document.createElement('div')
  // add the information
  // return the parent
}